/**
 * Counts the number of elements in an array
 * @param arr
 * @param type String to add after count
 * @returns string Number of elements in the array
 */
export function count(arr: unknown[]): string {
  return arr.length.toString();
}
